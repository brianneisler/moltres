#!/usr/bin/env node

const program = require('commander')

program
  .description('run a script')
  .requiredOption('-n, --name [name]', 'The name of the new project')
  .parse(process.argv)

const exec = async ({ name }) => {
  // TODO
}

exec(program)
  .then(() => process.exit())
  .catch((error) => {
    // eslint-disable-next-line no-console
    console.error(error)
    process.exit(1)
  })
